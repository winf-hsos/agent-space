requests
python-dotenv
pyyaml
croniter       # only needed for cron `schedules` in bots.yaml
pydantic-ai-slim[openai]   # agent runtime (OpenAI Responses API + native web search)

# Web UI
fastapi
uvicorn[standard]
jinja2
python-multipart   # required for FastAPI form parsing
