#!/bin/sh
# POSIX shim (Raspberry Pi / Linux / bash-like shell on Windows).
dir=$(dirname "$0")
if command -v python3 >/dev/null 2>&1; then
    exec python3 "$dir/chat_respond.py" "$@"
fi
exec python "$dir/chat_respond.py" "$@"
